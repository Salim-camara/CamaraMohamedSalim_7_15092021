import React from 'react';
import ReactDOM from 'react-dom';
import App from './LOGIN/App';
import './LOGIN/Styles/main.scss';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
